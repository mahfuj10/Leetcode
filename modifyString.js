const modifyString = function(s) {
    let str = ''
    for(let i = 0; i < s.length; i ++){
        if(s[i] === '?'){
          console.log(s[i])
        }
    }
    return str
};
console.log( modifyString('"j?qg??b"'));
 